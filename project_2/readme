Η εργασία υλοποιήθηκε από τους Γιώργο Μανδηλαρά k Θοδωρή Μανδηλαρά
Η εργασία δη είναι τελειοποιημένη
η εργασία δη διαχειρίζεται τις περιπτώσεις των float
ουσιαστικά στο κάθε block βάζουμε  char array
το data block είναι της μορφής d(δείκτης του επομενου block)\n("σοτιχειο"),(στοιχειο)\n κλπ
το d ειναι για να υποδικνυει οτι ειναι data block
τo στοιχειο είναι πάντα της μορφής char[]
το index block είναι της μορφής (δείκτης(σε char[])),(στοιχειο)\η κλπ και πάντα τελειώνει σε (δείκτη)

ο δείκτης είναι ένας "αριθμος" οποιος αντιστοιχεί στον αναγνωριστικό αριθμό του block που θέλουμε να δείξει


η ταξινόμηση παίρνει πάντα όλο το block k το ταξινομεί χρησιμοποιώντας τη bubblesort

στην insert στην αρχή εισάγουμε στο πρώτο  datablcok που είναι η ρίζα..μόλις γεμίσει γίνεται split k δημιουργείται ένα index block που ορίζεται ως ριζα
έπειτα όταν γεμίσει και το index block ξανακάνουμε split στ index block k το νέο block ορίζεται ως ριζα

το split υλοποιείται με τη χρήση στοίβας  για να να χούμε πρόσβαση στους πατέρες των κόμβων
ώστε άμα χωράνε να μπαίνουν αλλιώς να γίνονται k αυτοί split ετσι ώστε να ολοκληρωθεί η διαδικασία